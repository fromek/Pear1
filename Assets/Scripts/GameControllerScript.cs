using UnityEngine;
using System.Collections;

public class GameControllerScript : MonoBehaviour {

	
	// Update is called once per frame
	void Update () {
        //Mopved to Pause Script in UI Object
        //if (Input.GetButtonDown("Pause"))
        //{
        //    paused = !paused;
        //}
        //if (paused)
        //    Time.timeScale = 0;
        //else
        //    Time.timeScale = 1;
	}
}
